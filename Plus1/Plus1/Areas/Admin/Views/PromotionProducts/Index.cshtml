@model IEnumerable<Plus1.Areas.Admin.Models.PromotionProducts>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Visible)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Visible)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PromotionProductsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PromotionProductsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PromotionProductsID })
        </td>
    </tr>
}

</table>
